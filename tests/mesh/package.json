{
  "name": "navigator-test-mesh",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Feature tests for the Mesh SDK (@meshsdk/core), run by the navigator harness.",
  "scripts": {
    "test": "vitest run --reporter=json"
  },
  "dependencies": {
    "@meshsdk/core": "1.9.1"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
